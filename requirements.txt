# Star Graph Memory — Python dependencies
# v1.0.6

# Core (required)
numpy>=1.24.0
PyYAML>=6.0

# Embedding (required for semantic search)
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Multimodal / CLIP (optional, for cross-modal image+text memory)
# Install with: pip install -r requirements.txt[multimodal]
# torch>=2.0.0
# transformers>=4.30.0
# pillow>=9.0.0

# Dev & Testing
pytest>=7.4.0
pytest-cov>=4.1.0
